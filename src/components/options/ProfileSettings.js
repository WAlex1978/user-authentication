import React from 'react';
import { Text } from '../styled-components';
import Divider from '@material-ui/core/Divider';

const ProfileSettings = () => {
    return (
        <div>
            <Text bottom="10px">Profile Settings</Text>
            <Divider />
        </div>
    )
}

export default ProfileSettings;