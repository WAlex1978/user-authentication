import React from 'react';
import { Text } from '../styled-components';
import Divider from '@material-ui/core/Divider';

const UserSettings = () => {
    return (
        <div>
            <Text bottom="10px">User Settings</Text>
            <Divider />
        </div>
    )
}

export default UserSettings;